dist/
dev-dist/
coverage/
playwright-report/
test-results/
node_modules/
public/icons/
# Third-party plugin bundles, served verbatim.
public/plugins/
package-lock.json

# Generated by `npm run functions`, and checked byte-for-byte by the build.
# Formatting it here would make the generator and the formatter disagree, and
# the check would then fail after every `prettier --write`.
docs/FUNCTION_LEDGER.md

# The compiled core and its worklet shim. Emitted by `motionwave/wasm/build.sh`
# and compared against a fresh compile by `npm run wasm:check`, so reformatting
# them makes the formatter and that check disagree — the same argument the
# Function Ledger is here for, and the reason both are one line rather than an
# exemption somewhere clever.
motionwave/wasm/prebuilt/

# Emitted by `npm run curve:golden` from the C++ curve law and compared against
# a fresh emit by `npm run curve:check`.
motionwave/ui/test/curve_golden.json

# Written by `npm run soak` against one running build, and `npm run
# docs-guard:release` compares the bundle it names against `dist/`. The same
# argument as the Function Ledger above: a formatter that rewrites a generated
# file makes the artefact in git something the generator does not produce, and
# every comparison against it stops meaning anything.
docs/audit/SOAK.md
docs/audit/soak-coverage.json

# Written by `tests/storeSweep.test.ts` on every full `npm test`. Same argument
# as the two above: a formatter that rewrites a generated file makes the
# artefact in git something the generator does not produce.
docs/audit/store-coverage.json

# Written by `npm run reachability`. Same argument again, and this one has now
# cost a red `format:check`: the file was not listed here, so every sweep left
# the tree unformatted until somebody remembered to run the formatter over a
# document the formatter has no business rewriting. Its tables are read by
# `npm run route` and `route:check`, which parse the generator's own spacing.
docs/audit/REACHABILITY.md

# The local build record. Written by `npm run build`, gitignored, and not a
# document anybody reads — formatting it is churn on a file that is rewritten
# in full on every build.
.build-tree.json
