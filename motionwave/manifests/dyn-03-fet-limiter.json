{
  "$comment": [
    "The FET Limiter's parameters, declared once.",
    "",
    "Both the C++ dispatch and the TypeScript control table are generated from",
    "this file. See motionwave/manifests/fx-01-motion-shaper.json for why.",
    "",
    "Two things are absent because the hardware has neither: a threshold control",
    "and a ratio number. The threshold is fixed and INPUT is how hard the signal",
    "is driven into it; the ratio buttons change the feedback network's division,",
    "which moves the threshold as well as the slope. Section 9 test 5 is the one",
    "that catches a model treating the buttons as a slope alone - at a fixed",
    "input, 20:1 must give LESS reduction than 4:1."
  ],
  "unit": "dyn-03",
  "className": "FetLimiter",
  "header": "../fet_limiter.h",
  "namespace": "mw::units",
  "tsPrefix": "fet_limiter",
  "tsTarget": "motionwave/ui/units/fet_limiter/params.gen.ts",
  "params": [
    {
      "id": 1,
      "symbol": "Input",
      "name": "Input",
      "control": "knob",
      "accessibleName": "Input",
      "unit": "Decibels",
      "min": -20,
      "max": 40,
      "def": 0,
      "taper": "Linear",
      "smoothingMs": 30,
      "note": "This unit has no threshold control. INPUT is how hard the signal is driven into a fixed threshold, which is why it is the first thing a user reaches for.",
      "apply": "u.setInputGain(std::pow(10.0, v / 20.0));",
      "delta": [0, 24]
    },
    {
      "id": 2,
      "symbol": "Output",
      "name": "Output",
      "control": "knob",
      "accessibleName": "Output",
      "unit": "Decibels",
      "min": -20,
      "max": 40,
      "def": 0,
      "taper": "Linear",
      "smoothingMs": 30,
      "note": "Outside the detector loop; section 9 test 13 asserts sweeping it moves the reduction by under half a decibel.",
      "apply": "u.setOutputGain(std::pow(10.0, v / 20.0));",
      "delta": [-6, 6]
    },
    {
      "id": 3,
      "symbol": "Attack",
      "name": "Attack",
      "control": "knob",
      "accessibleName": "Attack",
      "unit": "Linear",
      "min": 1,
      "max": 7,
      "def": 7,
      "taper": "Linear",
      "smoothingMs": 20,
      "note": "Panel scale 1 to 7, fully clockwise being 7 and FASTEST. Section 9 test 3 exists to catch a model that gave this the conventional sense, which is what a plug-in user would expect and what the hardware does not do.",
      "apply": "u.setAttack(v);",
      "delta": [1, 7]
    },
    {
      "id": 4,
      "symbol": "Release",
      "name": "Release",
      "control": "knob",
      "accessibleName": "Release",
      "unit": "Linear",
      "min": 1,
      "max": 7,
      "def": 4,
      "taper": "Linear",
      "smoothingMs": 20,
      "note": "Same reversed sense as ATTACK.",
      "apply": "u.setRelease(v);",
      "delta": [1, 7]
    },
    {
      "id": 5,
      "symbol": "Ratio",
      "name": "Ratio",
      "control": "switch",
      "accessibleName": "Ratio buttons",
      "unit": "Choice",
      "min": 0,
      "max": 4,
      "def": 0,
      "taper": "Stepped",
      "steps": 5,
      "choices": ["4:1", "8:1", "12:1", "20:1", "All In"],
      "smoothingMs": 0,
      "note": "The buttons move the threshold as well as the slope, and upward: at a fixed input, 20:1 gives LESS reduction than 4:1. The fifth position is every button at once, which is a different device rather than a fifth ratio.",
      "apply": "const int i = static_cast<int>(v + 0.5);\n        u.setRatio(i <= 0 ? FetRatio::R4\n                          : (i == 1 ? FetRatio::R8\n                                    : (i == 2 ? FetRatio::R12\n                                              : (i == 3 ? FetRatio::R20 : FetRatio::AllIn))));",
      "delta": [0, 3]
    },
    {
      "id": 6,
      "symbol": "Limiting",
      "name": "Limiting",
      "control": "switch",
      "accessibleName": "Limiting in or out",
      "unit": "Choice",
      "min": 0,
      "max": 1,
      "def": 1,
      "taper": "Stepped",
      "steps": 2,
      "choices": ["Off", "On"],
      "smoothingMs": 0,
      "note": "ATTACK at OFF on the hardware: the detector leaves and the unit is a line amplifier. The amplifier and both transformers stay in circuit.",
      "apply": "u.setLimiting(v > 0.5);",
      "delta": [0, 1]
    },
    {
      "id": 7,
      "symbol": "Variance",
      "name": "Variance",
      "control": "knob",
      "accessibleName": "Unit variance",
      "unit": "Percent",
      "min": 0,
      "max": 1,
      "def": 0,
      "taper": "Linear",
      "smoothingMs": 30,
      "note": "One scalar for every per-instance deviation, including the element's own two trims - section 3.8's Q BIAS and DIST TRIM, which are where a drifted unit of this design actually differs. Its render-delta gate is -105 dBFS, and the reason is the topology rather than a weak control: a feedback limiter drives to a fixed output level, so drift in the gain element is absorbed by the loop and only the distortion difference survives. Perturbing the trims moved this row from -112 to -94 dBFS; the remaining smallness is the loop doing its job.",
      "apply": "u.setVariance(static_cast<float>(v), 23u);",
      "delta": [0, 1],
      "deltaFloorDb": -105
    },
    {
      "id": 8,
      "symbol": "Oversampling",
      "name": "Oversampling",
      "control": "switch",
      "accessibleName": "Oversampling factor",
      "unit": "Choice",
      "min": 0,
      "max": 3,
      "def": 3,
      "taper": "Stepped",
      "steps": 4,
      "choices": ["Off", "2x", "4x", "8x"],
      "smoothingMs": 0,
      "note": "Defaults to 8x, and not for polish. The fastest attack is 20 microseconds, which is 0.88 of a sample at 44.1 kHz - at any lower factor the top of the ATTACK control stops being a control.",
      "apply": "const int i = static_cast<int>(v + 0.5);\n        u.setTier(i <= 0 ? FetLimiter::Tier::Off\n                         : (i == 1 ? FetLimiter::Tier::X2\n                                   : (i == 2 ? FetLimiter::Tier::X4 : FetLimiter::Tier::X8)));",
      "delta": [1, 3]
    },
    {
      "id": 9,
      "symbol": "Noise",
      "name": "Noise",
      "control": "knob",
      "accessibleName": "Noise floor",
      "unit": "Percent",
      "min": 0,
      "max": 1,
      "def": 1,
      "taper": "Linear",
      "smoothingMs": 30,
      "note": "Scales the published figure: 81 dB below the threshold of limiting.",
      "apply": "u.setNoise(v * 3.0e-5);",
      "delta": [0, 1],
      "deltaFloorDb": -100
    }
  ]
}
